# AI Team Hub v1.0 Architecture Consolidation Plan

> **Phase 0 — Analysis & Planning** (no code changes)

---

## 1. Current Execution Chain Map

```
┌──────────────────────────────────────────────────────────────────┐
│                        REQUEST ENTRY                              │
└──────────────────────────────────────────────────────────────────┘
         │                        │                      │
         ▼                        ▼                      ▼
   ┌──────────┐           ┌──────────┐          ┌───────────────┐
   │ /api/msg │           │ /v1/chat │          │/api/tasks/exec│
   └────┬─────┘           └────┬─────┘          └───────┬───────┘
        │                      │                        │
        ▼                      ▼                        ▼
  ┌─────────────────┐   ┌──────────┐           ┌──────────────┐
  │team_collaboration│   │ pipeline │           │ TaskExecutor │
  │ .generate_...()  │   │.run_...()│           │.execute_...()│
  └────────┬────────┘   └────┬─────┘           └──────┬───────┘
           │                 │                        │
           │     ┌───────────┘                        │
           │     │              ┌─────────────────────┘
           │     │              │
           ▼     ▼              ▼
     ┌──────────────────────────────┐
     │     ai_service.py            │
     │  stream_ai_response()        │
     │  (the ONE real LLM caller)   │
     └─────────────┬────────────────┘
                   │
                   ▼
          ┌──────────────────┐
          │  httpx → Provider│
          │  (OpenRouter etc)│
          └──────────────────┘

    ════════════════════════════════════════════════
            ADDITIONAL COMPONENTS (background)
    ════════════════════════════════════════════════

    ┌───────────────────────┐
    │  MemoryTaskHook       │ ← Task lifecycle events
    │  → MemoryBuffer        │
    │  → MemoryService       │  (raw SQLite)
    │  → MemoryIntelligence  │
    └───────────────────────┘

    ┌───────────────────────┐
    │  Collaboration Layer  │ ← UNUSED by main flows
    │  EventBus / SharedCtx │
    │  StateSync / Realtime │
    └───────────────────────┘

    ┌───────────────────────┐
    │  Workspace Layer      │ ← Registered /api/workspaces/*
    │  Workspace/WorkspaceMg│  but NOT integrated with task/chat
    │  WorkspaceMemory      │
    └───────────────────────┘

    ┌───────────────────────┐
    │  Orchestrator*        │ ← Mostly orphaned / import-only
    │  routing, diversity,  │
    │  observability        │
    └───────────────────────┘

    ┌───────────────────────┐
    │  Cloudflare Worker    │ ← Same API, own FSM, separate DB
    │  worker/index.ts      │  1479 lines, quality gates
    └───────────────────────┘
```

### 1.1 Real Call Graph (who imports whom)

```
main.py
 ├── database.py
 ├── models.py
 ├── cache.py
 ├── routes/channels.py
 ├── routes/teammates.py
 ├── routes/apikeys.py
 ├── routes/messages.py
 │    ├── ai_service.py
 │    ├── cache_prefix_builder.py
 │    ├── cache_warmup_service.py
 │    ├── team_collaboration.py  ← only from messages.py
 │    │    ├── ai_service.py
 │    │    ├── cache.py
 │    │    └── database / models / crypto (lazy)
 │    └── attachment_context/service
 ├── routes/maeos.py
 │    └── services/maeos.py
 │         ├── pipeline.py
 │         │    ├── ai_service.py
 │         │    └── orchestrator_prompts.py
 │         ├── runtime/*
 │         │    ├── scheduler.py  ← dead code (imported, never called)
 │         │    ├── retry_policy.py
 │         │    ├── trace.py
 │         │    ├── context_isolation.py  ← dead (import only)
 │         │    └── flow_control.py       ← dead (import only)
 │         └── orchestrator_diversity.py  ← imported but unused in flow
 └── routes/tasks.py
      ├── task_manager.py → task_state.py
      ├── task_executor.py
      │    ├── task_state.py
      │    ├── task_context.py
      │    ├── task_result.py
      │    ├── task_events.py
      │    ├── task_approval_service.py
      │    ├── task_policy.py
      │    ├── runtime/retry_policy.py
      │    └── runtime/trace.py
      │    └── maeos.py (MAEOS instance via _get_maeos)
      ├── task_plan_service.py
      ├── task_plan_review.py
      ├── task_hooks.py
      │    └── memory_event_handler.py → memory/*
      └── ...
```

---

## 2. Target Execution Chain Map (v1.0 Consolidated)

```
                    ┌───────────────────────────┐
                    │    ONE execution kernel    │
                    │   pipeline.run_pipeline()  │
                    │   (planner→exec→review)    │
                    └───────────┬───────────────┘
                                │
                    ┌───────────▼───────────────┐
                    │  ai_service.py             │
                    │  stream_ai_response()      │
                    └───────────┬───────────────┘
                                │
                    ┌───────────▼───────────────┐
                    │  httpx → Provider API      │
                    └───────────────────────────┘

    HIGHER-LEVEL WRAPPERS (thin, no duplications):

    ┌─────────────────────────────────────┐
    │  team_collaboration.py  → calls      │
    │  pipeline N times (once/teammate)    │  ← rewrite as loop
    └─────────────────────────────────────┘

    ┌─────────────────────────────────────┐
    │  MAEOS  → queue + worker pool       │  ← keep, remove FSM overload
    │  each worker calls pipeline          │
    └─────────────────────────────────────┘

    ┌─────────────────────────────────────┐
    │  TaskEngine → TaskExecutor          │  ← keep as-is but remove
    │  → calls MAEOS for each step        │    dead imported modules
    └─────────────────────────────────────┘

    ┌─────────────────────────────────────┐
    │  Cloudflare Worker                  │  ← keep, simplify FSM to
    │  → fetch backend or pipeline        │    pipeline calls
    └─────────────────────────────────────┘
```

---

## 3. Module Disposition

### A. KEEP (no changes, actively used)

| Module | Reason |
|--------|--------|
| `backend/main.py` | Entry point, clean |
| `backend/database.py` | Standard SQLAlchemy setup |
| `backend/models.py` | All ORM models in use |
| `backend/cache.py` | Used everywhere |
| `backend/crypto.py` + `security/crypto.py` | Encryption needs |
| `backend/services/ai_service.py` | The ONE real LLM caller |
| `backend/services/pipeline.py` | Core execution kernel |
| `backend/services/maeos.py` | Active, in use by tasks + maeos route |
| `backend/services/task/task_state.py` | DB CRUD core, no fat |
| `backend/services/task/task_manager.py` | Thin wrapper, keep |
| `backend/services/task/task_executor.py` | Core execution logic |
| `backend/services/task/task_hooks.py` | Extension point |
| `backend/services/task/task_result.py` | Result persistence |
| `backend/services/task/task_events.py` | Event logging |
| `backend/services/task/task_context.py` | Context building |
| `backend/services/memory/memory_service.py` | Memory persistence |
| `backend/services/memory/memory_event_handler.py` | Hook → memory bridge |
| `backend/services/memory/memory_types.py` | Type defs |
| `backend/services/memory/memory_intelligence.py` | Insight generation |
| `backend/services/memory/memory_insight.py` | Insight engine |
| `backend/services/memory/memory_insight_store.py` | Insight persistence |
| `backend/services/memory/memory_retriever.py` | Memory query |
| `backend/services/memory/memory_compressor.py` | Memory compression |
| `backend/services/memory/memory_ranker.py` | Memory ranking |
| `backend/services/runtime/retry_policy.py` | Used by task_executor |
| `backend/services/runtime/trace.py` | Used by task_executor |
| `backend/services/cache_prefix_builder.py` | Used by messages |
| `backend/services/cache_warmup_service.py` | Used by messages |
| `backend/services/model_sync.py` | Model listing |
| `backend/services/key_vault_service.py` | Key management |
| `backend/services/tool_gateway.py` | Tool access |
| `backend/services/embedding_service.py` | Semantic search |
| `backend/services/rag_pipeline.py` | RAG pipeline |
| `backend/services/attachment_service.py` | File parsing |
| `backend/services/attachment_context.py` | Context building |
| `backend/services/team_collaboration.py` | **But see B** |
| All routes except workspace | All actively used |
| `worker/index.ts` | **But see D** |

### B. MERGE & SIMPLIFY

| Module | Target | What to do |
|--------|--------|------------|
| `team_collaboration.py` | Rewrite to call `pipeline.run_pipeline()` N times | Remove direct `stream_ai_response()` calls; simplify ROLE_AXIS_PROMPTS to shared prompts; eliminate [NO_NEW_INFO] retry logic (let pipeline handle errors); remove inline API key resolution (use key_vault_service) |
| `pipeline.py` | Keep as single kernel | Already clean; no changes needed |
| `orchestrator_diversity.py` | Inline into MAEOS or remove | `DiversityReport` + `analyze_diversity()` imported by MAEOS but `analyze_diversity()` is never actually called in the execution path. Either wire it in or delete the import. **Ponytail: delete import, keep file as-is for reference.** |
| `orchestrator_prompts.py` | Simplify | Contains `SYSTEM_PROMPT`, `PLANNER_PROMPT`, `REVIEWER_PROMPT` — these are used by pipeline. Add `COLLABORATION_PROMPT` for team_collaboration's role axis. Remove old FSM-specific prompts (`FSM_ORCHESTRATOR_SYSTEM_PROMPT`, etc.) |

### C. MOVE TO LEGACY

| Module | Why legacy | Location |
|--------|------------|----------|
| `workspace.py` + `workspace.py` route | Visionary but disconnected. Full abstraction (Workspace → Thread → Message) registered as `/api/workspaces/*` but NOT connected to task engine, chat, or MAEOS. Only route handler uses it. 554 lines of dead orchestrator code. | `legacy/workspace/` |
| `workspace_memory.py` | Second memory system built on SharedContext — not used by any task or chat flow. Duplicates Memory Intelligence Layer's purpose. | `legacy/workspace/` |
| `collaboration/` (event_bus, shared_context, realtime) | `EventBus` + `SharedContext` + `StateSync` — imported by workspace.py but NOT by chat, tasks, or MAEOS. These were designed for multi-agent streaming but never wired in. | `legacy/collaboration/` |
| `team_context.py` | Data classes (TeammateContext, CoordinatorRequest) — imported by NOTHING. 59 lines. | `legacy/data_structures/` |
| `runtime/scheduler.py` | `Scheduler` + `ExecUnit` — imported by MAEOS's `__init__` but MAEOS uses its own `PriorityTaskQueue` + `FSMWorker`. The Scheduler class is never instantiated in any execution path. | `legacy/runtime/` |
| `runtime/context_isolation.py` | Imported by MAEOS, never called | `legacy/runtime/` |
| `runtime/flow_control.py` | Imported by MAEOS, never called | `legacy/runtime/` |
| `mcp_archive/` | MCP server and systemd unit — old MCP integration | stays where it is |

### D. DELETE

| Module | Risk | Rationale |
|--------|------|-----------|
| `orchestrator_observability.py` | **LOW** — `v1_observability.py` calls `get_observability()` → `.replay()` which doesn't exist on `Observability` class (it's a simple request logger, not a trace store). These observability endpoints return empty/truncated data. The module is a stub. `get_observability()` is also imported by MAEOS but never called by any execution path. | Replace with direct cache/model read from routes or remove. |
| `orchestrator_routing.py` | **LOW** — `ModelRouter`, `ExecutionBudget`, `CircuitBreaker`, `GlobalBounds`, `classify_task` — NOT imported by ANY module. Full 287 lines of "Phase 7/8/10" planning that never materialized. | Delete (can be resurrected from git). |
| `complexity_classifier.py` | **LOW** — Separate from `orchestrator_routing.classify_task()`. Has its own `ComplexityClassifier` with tokenizer. Not imported by task flow. | Delete (duplicate of routing's classify). |
| `migration.py` | **MEDIUM** — Called at startup to migrate plaintext keys. If encryption is stable (enforced for weeks), the migration resolves to zero rows each time. Remove after verifying no plaintext keys remain. | Delete after confirmation. |
| `crypto.py` (top-level `backend/crypto.py`) | **MEDIUM** — 2743 lines. Is `decrypt_value()` also defined in `security/crypto.py`? Check. If `security/crypto.py` is the canonical one, delete `backend/crypto.py` and update all imports. | Merge into `security/crypto.py` or delete. |

---

## 4. Deletion Risk Analysis

| File | Risk Level | Mitigation |
|------|------------|------------|
| `orchestrator_observability.py` | 🟢 None | No caller depends on its result — `.replay()` falls through to `hasattr` check |
| `orchestrator_routing.py` | 🟢 None | Zero imports across entire codebase |
| `complexity_classifier.py` | 🟢 None | Zero imports |
| `runtime/context_isolation.py` | 🟢 None | Import-only in maeos.py, module never instantiated |
| `runtime/flow_control.py` | 🟢 None | Import-only in maeos.py, module never instantiated |
| `runtime/scheduler.py` | 🟢 None | Import-only in maeos.py, `Scheduler()` instantiated inside `FSMWorker` but never used for dispatch; MAEOS has its own queue |
| `team_context.py` | 🟢 None | Zero imports |
| `collaboration/` (full dir) | 🟢 Low | Imported only by `workspace.py` (which moves to legacy). If anything else imports it, that also moves. |
| `workspace.py` | 🟢 Low | Route exists but no test uses it; frontend doesn't call it |
| `workspace_memory.py` | 🟢 Low | Only imported by workspace.py |
| `orchestrator_diversity.py` | 🟡 Medium | `DiversityReport` imported by MAEOS and `analyze_diversity()` imported — but NEVER called in execution. Removing the import from MAEOS is safe. |
| `crypto.py` (top-level) | 🟡 Medium | Need to verify `decrypt_value()` isn't the only import others use |
| `migration.py` | 🟡 Medium | Called at startup; check if any plaintext keys still exist |
| `orchestrator_prompts.py` | 🟡 Medium | Actively used by pipeline.py for `SYSTEM_PROMPT`, `PLANNER_PROMPT`, `REVIEWER_PROMPT`. Only DELETE the old FSM-specific prompts inside it. |

### Import Tree for Delete Candidates

```
Delete candidate            Imported by
─────────────────────────────────────────────────────────
orchestrator_observability  → maeos.py (import, never calls)
                            → v1_observability.py (calls .replay() which doesn't exist)

orchestrator_routing        → NO ONE (zero imports)

complexity_classifier       → NO ONE (zero imports)

runtime/context_isolation   → maeos.py (import only)
runtime/flow_control        → maeos.py (import only)
runtime/scheduler           → runtime/__init__.py → maeos.py (import; constructor called once)

team_context                → NO ONE (zero imports)

collaboration/event_bus     → realtime.py → workspace.py
collaboration/shared_context → workspace.py → workspace_memory.py
collaboration/realtime      → NO ONE directly; only collaboration/__init__.py

workspace.py                → route only; NOTHING else imports it
workspace_memory.py         → workspace.py only
```

### Verdict: All delete candidates are safe.
The only files that reference them are the files themselves, route registrations that never serve real requests, or import-only references that resolve to no-ops.

---

## 5. Cloudflare Worker Assessment

| File | Risk | Decision |
|------|------|----------|
| `worker/index.ts` (1479 lines) | 🟡 Medium | Contains full API route + DB schema duplicates + its own FSM (quality gates, recovery). **Keep** — it's the deployed production endpoint for Cloudflare. But simplify: remove internal FSM (quality gate + recovery system) that duplicates pipeline logic. Keep route handlers and LLM calling only. |
| `worker/index.mjs` (129K) | 🟢 None | Compiled output, delete on rebuild |
| `worker/schema.sql` | 🟢 None | Schema reference |

**Plan for Worker**: The Worker's internal quality-gate + recovery-system (lines 15–500) replicate what `pipeline.run_pipeline()` already does in the backend. If the Worker is supposed to be stateless orchestration, it should proxy to the backend rather than reimplement the FSM. Option: strip the Worker down to API route definitions that call the backend endpoint.

---

## 6. Files & Lines Impact Summary

### Phase 1: Move to Legacy (~1,620 lines moved, no deletions)

```
Move to legacy/:
  backend/services/workspace.py              (554 lines)
  backend/services/workspace_memory.py       (300 lines)
  backend/services/collaboration/             (3 files, ~400 lines)
    event_bus.py
    shared_context.py
    realtime.py
  backend/services/team_context.py            (59 lines)
  backend/services/runtime/scheduler.py       (180 lines)
  backend/services/runtime/context_isolation.py (~50 lines)
  backend/services/runtime/flow_control.py    (~80 lines)
```

### Phase 2: Delete (safe, zero imports)

```
Delete:
  backend/services/complexity_classifier.py
  backend/services/orchestrator_routing.py
  backend/services/orchestrator_observability.py
  backend/services/orchestrator_diversity.py  (or keep but un-import from maeos.py)
```

### Phase 3: Merge

```
Rewrite:
  backend/services/team_collaboration.py
    → replace direct stream_ai_response() calls with pipeline.run_pipeline() loops
    → remove inline API key resolution, use key_vault_service
    → simplify [NO_NEW_INFO] retry (removed, pipeline handles)

  backend/services/orchestrator_prompts.py
    → remove FSM-specific prompts
    → add COLLABORATION_PROMPT

  backend/services/maeos.py
    → remove imports of scheduler/context_isolation/flow_control
    → remove import of orchestrator_diversity
```

### Phase 4: Simplify Worker

```
  worker/index.ts
    → remove quality-gate + recovery-system (lines 15-500)
    → keep route handlers + DB calls
    → (optionally) proxy LLM calls to backend instead of direct fetch
```

---

## 7. File-Level Modification Checklist

### No-change files (KEEP)
Files that stay exactly where they are:

```
backend/main.py
backend/database.py
backend/models.py
backend/cache.py
backend/crypto.py → verify dedup with security/crypto.py
backend/security/crypto.py
backend/services/ai_service.py
backend/services/pipeline.py
backend/services/maeos.py (MINOR: remove dead imports)
backend/services/runtime/retry_policy.py
backend/services/runtime/trace.py
backend/services/task/* (all 17 files)
backend/services/memory/* (all 12 files)
backend/services/cache_prefix_builder.py
backend/services/cache_warmup_service.py
backend/services/model_sync.py
backend/services/key_vault_service.py
backend/services/tool_gateway.py
backend/services/embedding_service.py
backend/services/rag_pipeline.py
backend/services/attachment_service.py
backend/services/attachment_context.py
backend/services/orchestrator_prompts.py (MINOR: cleanup)
backend/services/team_collaboration.py (REWRITE: call pipeline instead of direct LLM)
backend/routes/* (all 15 files, MINOR: remove workspace.py route)
worker/schema.sql
legacy/mcp_archive/*
```

### Move operations

| File | From → To | 
|------|-----------|
| `backend/services/workspace.py` | → `legacy/workspace/workspace.py` |
| `backend/services/workspace_memory.py` | → `legacy/workspace/workspace_memory.py` |
| `backend/services/collaboration/` | → `legacy/collaboration/` |
| `backend/services/team_context.py` | → `legacy/data_structures/team_context.py` |
| `backend/services/runtime/scheduler.py` | → `legacy/runtime/scheduler.py` |
| `backend/services/runtime/context_isolation.py` | → `legacy/runtime/context_isolation.py` |
| `backend/services/runtime/flow_control.py` | → `legacy/runtime/flow_control.py` |

### Delete operations

| File | After verification |
|------|-------------------|
| `backend/services/complexity_classifier.py` | 🟢 Safe |
| `backend/services/orchestrator_routing.py` | 🟢 Safe |
| `backend/services/orchestrator_observability.py` | 🟢 Safe |
| `backend/services/orchestrator_diversity.py` | 🟡 Remove MAEOS import first |
| `backend/services/migration.py` | 🟡 Only after confirmation |
| `backend/crypto.py` | 🟡 Only after import dedup check |

### Modification operations

| File | Change |
|------|--------|
| `backend/services/maeos.py` | Remove imports: `orchestrator_diversity`, `scheduler`, `context_isolation`, `flow_control`. Remove `Scheduler()`, `ContextIsolation()`, `FlowControlEnforcer()` instantiation from `FSMWorker.__init__()` |
| `backend/services/orchestrator_prompts.py` | Remove `FSM_ORCHESTRATOR_SYSTEM_PROMPT` and related old FSM prompts. Keep `SYSTEM_PROMPT`, `PLANNER_PROMPT`, `REVIEWER_PROMPT`. Add `COLLABORATION_PROMPT`. |
| `backend/services/team_collaboration.py` | Rewrite `_call_single_teammate()` to call `pipeline.run_pipeline()` instead of `stream_ai_response()` directly. Remove inline API key resolution (replace with `key_vault_service.get_key_by_provider()`). |
| `backend/routes/workspace.py` | Remove router registration (routes move with workspace to legacy). |
| `worker/index.ts` | Remove quality-gate + recovery-system → keep only route handlers + DB + LLM call. |
| All move targets | Update `__init__.py` exports and remove `from backend.services.xxx import ...` statements. |

---

## 8. Risk Matrix

| Risk | Likelihood | Impact | Mitigation |
|------|-----------|--------|------------|
| A caller of a moved module exists | Low | Moderate | `git grep` for every module before moving; move only after confirming zero callers |
| `workspace.py` route used by frontend | Low | High | Check frontend code for `/api/workspaces` calls |
| `collaboration/` used by future feature | Medium | Low | It's in git history; `legacy/` is still accessible |
| Delete breaks unexpected import | Low | High | Run `python -c "from backend import *"` after each deletion; run test suite |
| `migration.py` still needed | Medium | Low | Check if any plaintext keys exist in DB before deleting |
| `crypto.py` dedup breaks encryption | Low | High | grep `from backend.crypto` vs `from backend.security.crypto` |

---

## 9. Migration Strategy

```
Phase 0 (CURRENT): Analyze & Plan ✓
  → You are here.

Phase 1: Move to Legacy
  → git mv workspace.py workspace_memory.py team_context.py collaboration/ runtime/scheduler.py ...
  → Remove route registrations for workspace
  → Fix __init__.py exports

Phase 2: Delete Dead Code
  → Delete complexity_classifier.py, orchestrator_routing.py, orchestrator_observability.py
  → Clean up MAEOS imports

Phase 3: Simplify Active Modules
  → Rewrite team_collaboration to call pipeline.run_pipeline()
  → Clean up orchestrator_prompts.py
  → Streamline MAEOS FSMWorker

Phase 4: Simplify Worker
  → Strip quality-gate + recovery-system from index.ts

Phase 5: Verify
  → Run full test suite
  → Test chat flow, task execution, memory hooks
  → Manual smoke test
```

---

## 10. Quick Reference: Keep / Merge / Legacy / Delete

```
KEEP (62 files)              MERGE (4 files)          LEGACY (7 modules)       DELETE (5 files)
───────────────────          ───────────────          ──────────────────       ────────────────
ai_service.py                team_collaboration.py    workspace.py             complexity_classifier.py
pipeline.py                  orchestrator_prompts.py  workspace_memory.py      orchestrator_routing.py
maeos.py (clean imports)     worker/index.ts          collaboration/*          orchestrator_observability.py
main.py                      crypto.py (dedup)        team_context.py          orchestrator_diversity.py*
database.py                                           runtime/scheduler.py     migration.py**
models.py                                             runtime/context_iso      crypto.py (top-level)**
cache.py                                              runtime/flow_control
security/*
all routes (15)
all task/* (17)
all memory/* (12)
runtime/retry_policy
runtime/trace
key_vault_service
model_sync
embedding_service
rag_pipeline
attachment_service
attachment_context
tool_gateway
cache_prefix_builder
cache_warmup_service
worker/schema.sql

* = remove import before deleting
** = verify before deleting
```
